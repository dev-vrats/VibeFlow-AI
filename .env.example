# Google Maps API Key
MAPS_API_KEY=mocked_maps_key

# Gemini / Vertex AI Project Details (or API Key if not using Vertex specific)
GEMINI_API_KEY=mocked_gemini_key

# Firebase settings
FIREBASE_CREDENTIALS_PATH=mocked_credentials.json
